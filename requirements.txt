# =========================================================
# maintenance_advisor — Dependências Python
# =========================================================
# Instale no mesmo ambiente Python que o Odoo usa:
#
#   sudo /path/to/odoo/venv/bin/pip install -r requirements.txt
#
# Ou, se o Odoo roda no sistema sem virtualenv:
#   sudo pip3 install -r requirements.txt --break-system-packages
# =========================================================

numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
xgboost>=2.0.0
shap>=0.44.0
joblib>=1.3.0
